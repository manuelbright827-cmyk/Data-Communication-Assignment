# PDF Embed Fix - Checklist

- [ ] Confirm PDF embed works via file URL and via simple local server.
- [ ] Apply robust PDF embedding approach (viewer fallback + <iframe> fallback).
- [ ] Ensure fallback text is visible even when <object> can’t render.
- [ ] Add a visible "Open PDF" link in the fallback region.



